# Shopper


### Download and installation

Install packages:

```powershell
npm install
```

Starting server and client:

```powershell
npm start
```

Start client only

```powershell
npm run client
```

Start Server only

```powershell
npm run server
```